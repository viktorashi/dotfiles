Please keep in mind that my dotfiles repo is managed using the `conf` bash function, which is a `git` wrapper.

Any change you make to configurations, please try to make it in such a way that it's safe and easy to track it to my dotfile.
