# mac

eval "$(/opt/homebrew/bin/brew shellenv)"

# Created by `pipx` on 2025-10-25
export PATH="$PATH:$HOME/.local/bin"

[ -f "$HOME/.config/broot/launcher/bash/br" ] && . "$HOME/.config/broot/launcher/bash/br"
