# ~/.makepkg.conf

# Point makepkg's internal git operations to our custom gitconfig
# which forces shallow clones (depth = 1) to avoid massive downloads
MAKEPKG_GIT_CONFIG="$HOME/.config/makepkg/gitconfig"
